# Test 1: Baseline — 500,000 messages
# Metrics to collect: write throughput, DB CPU/mem, write latency percentiles, queue depth
test.name=baseline
test.description=500K messages baseline test

test.total.messages=500000
client.threads=256
client.target.rooms=20

# Run command:
# java -jar client-test/target/chat-test-client.jar \
#   ws://${ALB_HOST}/ws/chat 256 500000 http://${ALB_HOST}
