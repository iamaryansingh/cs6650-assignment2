# Test 3: Endurance — 30 minutes sustained load
# Monitor for: memory leaks, connection pool exhaustion, disk space, performance degradation
test.name=endurance
test.description=30 minute sustained endurance test

test.duration.minutes=30
client.threads=128
client.target.rooms=20

# Target rate: 80% of maximum throughput observed in Test 1
# Adjust client.threads based on Test 1 results to hit ~80% rate

# Run command (send messages continuously for 30 min, adjust total to match):
# java -jar client-test/target/chat-test-client.jar \
#   ws://${ALB_HOST}/ws/chat 128 <total-for-30min> http://${ALB_HOST}
